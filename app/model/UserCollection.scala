package model

import java.util.Calendar

import model.User
import model.Email
import java.util.concurrent.ConcurrentHashMap

import org.joda.time.DateTime

import scala.collection.mutable.ListBuffer


class UserCollection
{
//  val Alice = new User(1, "Alice", "airoboy@mail.ru", Option(ListBuffer(new Email("Alice", "Bob", "Hello", "HelloWorld"))))
//  val Bob = new User(2, "Bob", "kabirov.airat@inbox.ru", Option(ListBuffer()))
//  val Eve = new User(3, "Eve", "airoboy777@gmail.com", Option(ListBuffer()))
//
//  val users = List(Alice, Bob, Eve)
//  val emails = List()
}
